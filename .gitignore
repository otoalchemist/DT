node_modules/
dist/
release/
*.log

# Local editor / agent config
.claude/

# Secrets & local state — NEVER commit
.env
.env.*
!.env.example
data/settings.json
data/activity.json
data/config.json
# Legacy default-list sync bookkeeping; ignored so old installs can retain it harmlessly
data/.list-sync.json
# Legacy self-updater artifacts; ignored so old installs do not accidentally commit them
data/.update-manifest.json
.update-backup/
data/flashbots-signer.key
data/*.keystore.json
keystore/
