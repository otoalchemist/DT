node_modules/
dist/
release/
*.log

# Local editor / agent config
.claude/

# Secrets & local state — NEVER commit
.env
.env.*
!.env.example
data/settings.json
data/activity.json
data/config.json
# Emigration treasury ledger: the scanned movements plus YOUR roster of who has
# pledged, what to call them, and which movements don't count. Local state.
data/treasury.json

# Published static treasury page — regenerate with `npm run publish-treasury`
site/dist/
# Hashes of the last auto-synced default lists (local sync bookkeeping, see list-sync.ts)
data/.list-sync.json
# Race telemetry: local experiment data about this install’s own submissions
data/race-timing.jsonl
# Self-updater bookkeeping: what the last update installed, and the files it replaced
# (see scripts/update.mjs). Both are local state about THIS install.
data/.update-manifest.json
.update-backup/
data/flashbots-signer.key
data/*.keystore.json
keystore/
