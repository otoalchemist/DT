{
  "name": "@dat-bot/backend",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "postmortem": "tsx src/postmortem.ts",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run --exclude \"dist/**\" --exclude \"**/*.anvil.test.ts\"",
    "test:integration": "forge build --root test/foundry && forge build --root ../.. --contracts contracts --out packages/backend/test/foundry/out/payer-compiled --cache-path packages/backend/test/foundry/cache/payer-compiled --use 0.8.20 --evm-version shanghai --optimize --optimizer-runs 200 --no-metadata && vitest run anvil --exclude \"dist/**\"",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@dat-bot/shared": "*",
    "@fastify/websocket": "^11.0.1",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "viem": "^2.21.45",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10",
    "fast-check": "^3.23.2"
  }
}
